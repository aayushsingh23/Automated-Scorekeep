pipeline {
    agent any
    
    tools {
        // Specify the correct Node.js installation name
        nodejs 'Node.js'
    }
    
  stages {
        stage('Build') {
            steps {
                echo "Print Hora"
                // Add more build steps as needed
            }
        }
    }
}
