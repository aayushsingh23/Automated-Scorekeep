pipeline {
    agent any
    
    triggers {
        cron('H * * * *') // Runs every hour
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                

            }
        }
    }
}
